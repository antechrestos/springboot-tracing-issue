# Springboot tracing issue demo

## Run the project

### Infrastructure

Go to `infrastructure` and run `docker compose up`

### Application

Run the application, everything is configure to match the infrastructure